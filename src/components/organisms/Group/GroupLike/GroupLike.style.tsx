import styled from "styled-components";

const GroupLikeButton = styled.button`
  display: flex;
  align-items: center;
  margin-bottom: 2px;
  margin-right: 3px;
`;

const styledComponent = {
  GroupLikeButton,
};

export default styledComponent;
